'use client';

/**
 * Owner onboarding preview — same UI as /v/[id], different data source
 * (/api/menu/[id]/preview) when pathname starts with /preview/.
 */
export { default } from '../../v/[id]/page';
